{
  "name": "vscode-evo-dev",
  "version": "1.0.0",
  "description": "Development dependencies for Evochora VS Code extension",
  "private": true,
  "devDependencies": {
    "@vscode/vsce": "^3.2.1"
  },
  "scripts": {
    "build": "cd extension && vsce package --out ../evochora-syntax.vsix"
  }
}
